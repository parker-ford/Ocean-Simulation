#pragma kernel CSMain

float resolution;
Texture2D<float2> freqData;
RWTexture2D<float4> pingpong;

[numthreads(1,1,1)]
void CSMain (uint3 id : SV_DispatchThreadID)
{
    float2 freq = freqData[id.xy];
    float4 data = float4(freq, 0, 0);
    pingpong[id.xy] = data;
}
